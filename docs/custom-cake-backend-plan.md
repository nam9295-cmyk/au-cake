# Custom Cake backend execution plan

## Global constraints

Source of truth: docs/custom-cake-api-contract.md, src/lib/custom-cake-contract.ts, src/lib/cake-order-v2-contract.ts, src/lib/custom-cake-photo-contract.ts and tests/fixtures/custom-cake-contract/* at a42f82f9b2fbd34e1091f8d3cce02c00e7224fb3 (includes caa9a96). Also approved local au-cake-custom-cake-plan.md; detailed wire document wins if inconsistent. Do not change the contracts or original fixtures; stop and report concrete contract defects.

Only feat/custom-cake-backend at /home/john/workspace/au-cake/.worktrees/custom-cake-backend via ssh very. No frontend feature code, integration/main feature merge, deployment or operational DB/migration execution. Existing stored-order readers/catalog/policy, v1 canonical/fingerprint/price replay and Stage 0 golden17 immutable. Compat v1 pricing stays until required gate; never enable required operationally. Small commits after targeted+contract+golden+affected archive tests. TDD RED before implementation. No test weakening. No subagents from implementers. Use apply_patch for edits (local scratch plus scp if remote apply_patch unavailable); remote Node PATH /home/john/.nvm/versions/node/v24.14.0/bin. rg unavailable remote: grep/find fallback.

## Task 1 — Pure new order core

Read all binding contract/type files and four original fixtures plus supplement; implement ONLY pure input, canonical/fingerprint, pricing/data construction for custom-cake.v1 and cake-order.v2. Existing current core files are cake-order-catalog.js, cake-order-input.js, cake-order-pricing.js, cake-order-data.js. Add explicit versioned exports there, with focused new pure modules if needed; do not change existing exports/semantics. No main actions/persistence/photos/adapters yet. New pricing must never import stored-order-* or use compatibility mode. Legacy v1 remains untouched.

TDD tests in tests/custom-cake-order-core.test.mjs covering actual exports: six prices15500/20000/25000/25500/36500/47500; first persisted receipt before 2026-09-30T14:00:00.000Z custom base-only5% each-line rounding, gifts2/item; expiry no gifts/5%; null/0/positive quotes; standalone450 vs addon315 per stick including1/6/12; parent missing/self/smore/inactive reject; unknown caller pricing reject; stable line IDs and sorting no merging but aggregate cake max5; positive safe cents and overflow; normalized canonical exact original strings+HMAC synthetic key for both domains, reorder same, changing parent/IDs different; ordinary required-catalog options/packaging/coupons current rounding retained, no bulk/coupon on smore. Contact normalization and strict selected-variant fields per wire. Keep clock validation for NEW submit separate from canonical replay (past pickup must not prevent matching replay). Return wire-conformant data for fixtures. New request IDs lowercase UUIDv4. Pure fingerprint accepts injected server secret, must validate/use existing secret conventions.

Expose clear normalized request/canonical/pricing/data exports and report signatures for later tasks. Quote calculation should be reusable for immutable bases plus extras edits. No test-only shortcuts in production. Add npm test hookup. If new relative dependency affects exported facade archive imports, update archive inclusion in same commit and verify actual extracted import fixture. Run targeted tests+contract+golden+artifact and npm test before final task commit. Split responsibilities into small commits; include RED/GREEN commands/output in report. Commit only owned files plus this implementation plan if it is untracked. Report to .superpowers/sdd/custom-cake-backend-plan/task-1-report.md.

## Task 2 — Additive persistence and migration

Build separate custom request/quote and v2 snapshot records (never custom inside orderLinesJson.version1), transaction repository adapters, durable session/photo/transition/notification metadata. Read existing Appwrite SDK transaction and migration helpers. Implement additive-only migration runner dry-run default, exact expected drift fail-closed, apply reconfirmation, no delete/update/backfill existing data. Test mocked service HTTP with full schema fixtures and no mutations on dry-run/drift; never run apply against production. Transaction CAS+idempotent creation needs one requestId namespace with wire+creator scope, persisted original creation result, quote/acceptance/history and terminal transition audit. Use current SDK transactions, do not simulate CAS by read/update outside a transaction. Tests include concurrent conflicts and uncertain-commit reconciliation. No public Storage permissions. New config must default off/unavailable until provisioned; operational environment untouched.

## Task 3 — Private photos

Implement custom-cake-photo.v1 exact contracts; server bearer session30min, ownership not phone/requestId alone, metadata intent before upload, real decode/signature checks JPEG/PNG/WebP10MiB20MP singleframe, stripmetadata WebPmax2560, max5 across races, uploadId idempotency, attach fence and authoritative24hcleanup. Reuse installed review-api image library patterns after version check; test real image bytes and fake repositories only at service boundaries. Handle request authentication for attached read/delete with tombstones and no request corruption. Read/write credentials absent from canonical and logs. Tests for expiry, fake identity, malformedimages, full bounds, concurrentupload/attachcleanup, uncertain/failedstorage and durable retries. No production Storage calls.

## Task 4 — Actions, workflow and cutover

Register exact new actions in reservation API via focused orchestration; use new pure core. Implement create/lookup/quoteupdate/accept/confirm/complete/cancel with existing administrator auth, full customer lookup possession/privacy controls, atomic expectedStatus+expectedQuoteVersion checks, exact recorded no-op terminal replay. Quote revision preserves stale acceptance history; cannot confirm outdated agreement. Coupon transaction/reconciliation remains semantically equivalent and v1 flow not redesigned. Explicit compat/required gate oldnew only after existing replay; valid existing v1 samepayload retains fingerprints/prices, mismatchedrequest conflict. Capability fails closed for missing resources. New errors exact version-specific envelope; oldhealth unchanged. Tests through actual handler, repositories and controlled transactions; no deployed API calls.

## Task 5 — Adapters and notifications

Separate src/lib/custom-cake-client.ts/custom-cake-repository.ts and v2 adapters if needed, no pricing/parsing back into repository.ts. Preserve creation/lookup/health separation. Reception/quote/confirmation persisted events with existing email delivery idempotency/retry; no implicit email on terminal transition or replay. Tests through real parsers/adapters/event delivery code and controlled transports. No UI/router/product exposure. Confirm artifacts include all transitive runtime dependencies, private config and required native deps.

## Task 6 — Final verification and review

Run all new targeted suites, immutable contracts/golden17, full npm test, lint, AU/KR builds, reservation-api compatibility/full, notification/reminder extracted archives standalone imports+fixtures. Review full diff vs common a42f82f, legacy exports/canonical unchanged, private resource migration dry-run guards. Resolve task/final review findings. Report architecture/migration/actions/pricing/cutover/tests/commits/final SHA. No feature push/merge/deploy without further authorization. Integration baseline push to a42f82f was explicitly authorized and completed separately.
